# Here are the note we should consider for Odoo DEV

- installing `libldap2-dev libsasl2-dev` in the host.
- `chmod +x ./odoo-bin`
- `sudo apt install postgresql postgresql-client`
- `pip install pyzk`
